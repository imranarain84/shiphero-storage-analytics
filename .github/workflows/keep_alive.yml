name: Keep App Alive

on:
  schedule:
    - cron: '*/20 * * * *'  # every 20 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s -o /dev/null -w "%{http_code}" https://vp-sh-storage-report-gfl95.ondigitalocean.app/
